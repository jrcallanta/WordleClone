{
  "name": "server",
  "version": "1.0.0",
  "description": "This is the server for a WordleClone project.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "build": "cd ../client && (npm install && npm run build)"
  },
  "author": "Jelito S Callanta II",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.0",
    "express": "^4.18.1"
  }
}
